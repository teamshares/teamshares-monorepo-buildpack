#!/usr/bin/env bash

# # Note: when it DOES fail, it doesn't seem to actually echo these lines to the log...?

if [ ! -z "$APP_SUBDIR" ]; then
  echo "Missing APP_SUBDIR in env -- must be set as a Heroku config var"
  exit 1
else
  echo "Teamshares monorepo buildpack"
  exit 0
fi
